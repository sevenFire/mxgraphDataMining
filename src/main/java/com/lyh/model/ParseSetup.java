package com.lyh.model;

public class ParseSetup extends Vertex {
    public ParseSetup(String vertexLabel) {
        super(vertexLabel);
    }

    public ParseSetup() {
        super("parse setup");
    }
}
